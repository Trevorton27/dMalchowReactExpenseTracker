import React from "react"

function Header() {
    return (
        <header className="App-header">
            <h1>Personal Expense Tracker</h1>
            <h3>Input your data into the fields then click the button 'Add to list'</h3>
        </header>
    )
}

export default Header